// ─── HRM initial/mock data constants ──────────────────────────────────
// Tách từ HumanResourcesManagement.tsx
// Dữ liệu khởi tạo hiện để rỗng — toàn bộ dữ liệu được lưu trên database / localStorage

import { Role, DepartmentCriteria } from './hrTypes';

export const INITIAL_ROLES: Role[] = [];

export const DEFAULT_DEPARTMENT_CRITERIA: DepartmentCriteria[] = [];
